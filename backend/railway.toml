[build]
builder = "NIXPACKS"
buildCommand = "npm install --ignore-optional"

[deploy]
startCommand = "node server.js"
healthcheckPath = "/api/stats"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
